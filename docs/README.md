# Sheets & Sorcery — Web

Browser version of the game.

## Run

```bash
# From project root
npx serve docs -p 3333
# Open http://localhost:3333
```

Or open `docs/index.html` directly (ESM may require a local server in some browsers).
